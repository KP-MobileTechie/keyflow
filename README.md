# keyflow

A typing-speed trainer with a terminal aesthetic — live WPM, per-key error heatmap, local history. Work in progress.

## Development
```bash
npm install
npm run dev    # http://localhost:3000
npm test       # engine unit tests (Vitest)
```
